# 🏗️ Android App Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Face ID Android App                      │
│                                                              │
│  ┌────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Enroll    │  │   Verify     │  │   Manage     │        │
│  │   Face     │  │    Face      │  │  Database    │        │
│  └────────────┘  └──────────────┘  └──────────────┘        │
│         │                │                  │                │
│         └────────────────┴──────────────────┘                │
│                         │                                    │
│              ┌──────────▼──────────┐                         │
│              │    MainActivity     │                         │
│              └─────────────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           UI Layer                                   │
│  ┌─────────────┐  ┌────────────────┐  ┌─────────────────────────┐  │
│  │  Main       │  │  Enrollment    │  │  Verification           │  │
│  │  Activity   │  │  Activity      │  │  Activity               │  │
│  └─────┬───────┘  └────┬───────────┘  └─────┬───────────────────┘  │
└────────┼────────────────┼────────────────────┼──────────────────────┘
         │                │                    │
         │                │                    │
┌────────▼────────────────▼────────────────────▼──────────────────────┐
│                        Business Logic                                │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                   CameraX Integration                         │   │
│  │  • Preview                                                    │   │
│  │  • Image Capture                                              │   │
│  │  • Lifecycle Management                                       │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────┐         ┌────────────────────────────────┐   │
│  │  FaceDetector    │         │  FaceRecognitionModel          │   │
│  │                  │         │                                │   │
│  │  • ML Kit        │         │  • PyTorch Mobile              │   │
│  │  • Face Detect   │◄────────┤  • Load Model                  │   │
│  │  • Crop Face     │         │  • Extract Embeddings          │   │
│  │                  │         │  • Cosine Similarity           │   │
│  └──────────────────┘         └────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    FaceDatabase                              │   │
│  │  • Add Face                                                  │   │
│  │  • Delete Face                                               │   │
│  │  • Get All Faces                                             │   │
│  │  • JSON Serialization                                        │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
         │
         │
┌────────▼──────────────────────────────────────────────────────────┐
│                        Data Layer                                  │
│  ┌────────────────────┐      ┌─────────────────────────────────┐  │
│  │  face_database.json│      │      model.pt (assets)          │  │
│  │  (App Private Dir) │      │      (TorchScript)              │  │
│  └────────────────────┘      └─────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
```

## Data Flow

### Enrollment Flow

```
User Input (Name)
       │
       ▼
Start Camera Preview
       │
       ▼
┌──────────────────────┐
│   For each pose:     │
│   1. Capture Image   │◄───┐
│   2. Detect Face     │    │
│   3. Crop Face       │    │
│   4. Extract Embed   │    │
│   5. Store Embed     │    │
└──────┬───────────────┘    │
       │                    │
       ├────────────────────┘
       │ (Repeat 5 times)
       ▼
Save to Database
       │
       ▼
Show Success
```

### Verification Flow

```
Start Camera Preview
       │
       ▼
Capture Image
       │
       ▼
Detect Face (ML Kit)
       │
       ▼
Crop Face (with padding)
       │
       ▼
Extract Embedding (PyTorch)
       │
       ▼
┌────────────────────────────┐
│  For each stored face:     │
│  1. Load embeddings        │◄───┐
│  2. Calculate similarity   │    │
│  3. Track max similarity   │    │
└────────┬───────────────────┘    │
         │                        │
         ├────────────────────────┘
         │ (All faces)
         ▼
Compare with Threshold (0.6)
         │
    ┌────┴────┐
    ▼         ▼
  Match    No Match
    │         │
    ▼         ▼
✅ Grant   ❌ Deny
  Access    Access
```

## Module Dependencies

```
MainActivity
    ├── EnrollmentActivity
    │   ├── CameraX
    │   ├── FaceDetector (ML Kit)
    │   ├── FaceRecognitionModel (PyTorch)
    │   └── FaceDatabase (JSON)
    │
    ├── VerificationActivity
    │   ├── CameraX
    │   ├── FaceDetector (ML Kit)
    │   ├── FaceRecognitionModel (PyTorch)
    │   └── FaceDatabase (JSON)
    │
    └── DatabaseActivity
        ├── FaceDatabase (JSON)
        └── FaceListAdapter (RecyclerView)
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│           Presentation Layer            │
│  • Material Design Components           │
│  • View Binding                         │
│  • Kotlin Coroutines                    │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│            Domain Layer                 │
│  • Face Detection (ML Kit)              │
│  • Face Recognition (PyTorch Mobile)    │
│  • Database Management (Gson)           │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│             Data Layer                  │
│  • Local JSON Storage                   │
│  • TorchScript Model Assets             │
│  • Camera (CameraX)                     │
└─────────────────────────────────────────┘
```

## Processing Pipeline

### Image Preprocessing

```
Raw Camera Image
       │
       ▼
Face Detection (ML Kit)
       │
       ▼
Bounding Box + Padding (20%)
       │
       ▼
Crop to Face Region
       │
       ▼
Resize to 224x224
       │
       ▼
Convert to Tensor
       │
       ▼
Normalize (mean/std)
       │
       ▼
Ready for Model
```

### Embedding Extraction

```
Preprocessed Tensor [1, 3, 224, 224]
       │
       ▼
PyTorch Mobile Model
       │
       ├─► Feature Extraction (MobileNetV2)
       │
       ├─► Embedding Layer
       │
       └─► L2 Normalization
              │
              ▼
      Embedding [1, 128]
```

### Similarity Comparison

```
Test Embedding [128]
       │
       ▼
┌──────────────────────────┐
│  For each stored face:   │
│                          │
│  Stored Embedding [128]  │
│          │               │
│          ▼               │
│  Cosine Similarity       │
│          │               │
│          ▼               │
│  Similarity Score [0-1]  │
└──────────┬───────────────┘
           │
           ▼
    Max Similarity
           │
           ▼
  Compare with Threshold
      (default: 0.6)
           │
      ┌────┴────┐
      ▼         ▼
    Match    No Match
```

## Threading Model

```
┌─────────────────────────────────────────┐
│           Main Thread (UI)              │
│  • User interactions                    │
│  • Display updates                      │
│  • Dialog management                    │
└────────┬────────────────────────────────┘
         │
    Dispatches to
         │
┌────────▼────────────────────────────────┐
│        Background Threads               │
│  ┌─────────────────────────────────┐   │
│  │  CameraX Executor               │   │
│  │  • Image capture                │   │
│  │  • Frame processing             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Coroutine Dispatchers.Default  │   │
│  │  • Face detection               │   │
│  │  • Embedding extraction         │   │
│  │  • Similarity calculation       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Coroutine Dispatchers.IO       │   │
│  │  • Database operations          │   │
│  │  • File I/O                     │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────┐
│         Optimization Strategies         │
│                                         │
│  1. Model Optimization                  │
│     • TorchScript compilation           │
│     • MobileNetV2 (efficient arch)      │
│     • Quantization ready                │
│                                         │
│  2. Camera Optimization                 │
│     • CAPTURE_MODE_MINIMIZE_LATENCY     │
│     • Efficient preview resolution      │
│     • Single camera instance            │
│                                         │
│  3. Memory Optimization                 │
│     • Bitmap recycling                  │
│     • ImageProxy quick close            │
│     • Lazy model loading                │
│                                         │
│  4. Threading Optimization              │
│     • Async face detection              │
│     • Background embedding extraction   │
│     • UI updates on main thread only    │
└─────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────┐
│          Security Layers                │
│                                         │
│  1. App Sandbox                         │
│     • Private app directory             │
│     • No external access                │
│                                         │
│  2. Permission System                   │
│     • Runtime camera permission         │
│     • User-granted access               │
│                                         │
│  3. Data Protection                     │
│     • Android encryption               │
│     • No network transmission           │
│     • Local-only processing             │
│                                         │
│  4. No Reverse Engineering              │
│     • ProGuard obfuscation             │
│     • Binary model format               │
└─────────────────────────────────────────┘
```

## Future Extensibility

```
Potential Enhancements:
├── Multiple face enrollment per person
├── Face liveness detection
├── Age/emotion estimation
├── Cloud backup option
├── Multi-language support
├── Accessibility features
├── Face grouping/clustering
└── Analytics dashboard
```
